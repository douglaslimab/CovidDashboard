# CovidDashboard

Development of a web application which read covid data in a CSV file and show it on the dashboard, according to the country added in the end point its map is shown on a section on the side.

# Technologies used
  -> Frontend
    HTML,
    CSS,
    Vanilla Javascript,
    
  -> Backend
    Python,
    Flask,
    
  -> Database
    *
    
  -> APIs
    Google Maps
